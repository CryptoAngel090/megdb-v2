# PROGRESS.md — MegDB

**Current status:** Active development — web app, API, design tokens baseline.

**Last focus:** Ultra stabilization pass: fixed `MovieComments` submit-flow lint blocker, resolved residual web lint errors from export-cleanup fallout, restored green `knip`, and re-verified full root `pnpm run build`.

**Next:** Continue Stage 2 modernization prep from a green baseline (`web lint` + `knip` + root `build`), then begin toolchain migration slices (Node/TS/Next/Biome CI gates) in atomic batches.

---

## Recent log (append one line per meaningful session step)

- 2026-04-28 — Ultra stabilization: fixed `MovieComments` submit handler pattern, resolved residual `tmdb` helper lint fallout, recovered green `knip`, and validated full monorepo build after clearing stale `apps/web/.next` artifacts.
- 2026-04-28 — Movie detail primary summary polish: split dense meta line (box office now secondary), toned down TMDB lockup against MegDB rating focus, softened icon-action contrast, and improved synopsis width/line-height for cleaner readability.
- 2026-04-28 — Stage 1 cleanup batch #3: removed residual knip unused exports/types in API+web modules (`tmdb`/discover/entity helpers) and reached green `pnpm run knip -- --reporter compact --max-show-issues=200` with `turbo type-check` still passing.
- 2026-04-28 — Stage 1 cleanup batch #2: removed all knip-reported unused deps/devDeps (`apps/web`, `apps/admin`, `apps/api`, `packages/db`, `packages/ui`, root); refreshed `.reports/knip-baseline.json`; current knip payload is export/type-heavy.
- 2026-04-28 — Stage 1 baseline report finalized: fixed workflow YAML parse blocker for `knip` and exported `.reports/knip-baseline.json` for cleanup planning.
- 2026-04-28 — Cleared all active local host servers (`:3000/:3100/:5000`), pruned pnpm cache, and launched a clean new web host on `:3000` for fresh Lighthouse/perf runs.
- 2026-04-28 — Ultra Lighthouse pass: added `/categories` SEO contract metadata+JSON-LD, disabled card prefetch fan-out, switched metadata icons/logo to `/icon`, added `/notifications` route to remove 404s, and improved SearchBar/Hero/MediaCard accessibility; production Lighthouse now `SEO 100`, `Best Practices 100`, `Accessibility 91`, `Performance 49`.
- 2026-04-28 — Stage 1 bootstrap complete: installed `knip`/`biome`, added `biome.json` + `knip.json` + root scripts, captured initial static-cleanup debt, and documented current blocker (`knip` parse error on `.github/workflows/generate-manifest.yml`).
- 2026-04-28 — Stage 0 baseline snapshot complete: counted repo files/dirs, captured runtime versions, verified `type-check`/`test` pass, logged current `build` failure (`MovieComments.tsx` no-misused-promises), and recorded missing `knip` in `CHECKLIST.md`.
- 2026-04-28 — `/register` QA pass: page responds 200, API registration success/conflict verified, copy normalized to `Username`, removed inline style residue, and improved error fallback to include HTTP status when API error body is missing.
- 2026-04-28 — Inserted Google OAuth credentials into `apps/web/.env.local`, restarted Next.js on `:3000`, and validated signin route response; still `302 /login?...&error=google`, so the blocker is now Google Console config/credential validity.
- 2026-04-28 — Diagnosed `error=OAuthSignin`: `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` were empty in `apps/web/.env.local`; updated `apps/web/.env.example` to document required server-side OAuth keys and NextAuth config.
- 2026-04-28 — Added RegisterForm OAuth guards: Google/GitHub social buttons now disable when `NEXT_PUBLIC_*_CLIENT_ID` is missing and show clear setup error instead of misleading redirect back to login.
- 2026-04-28 — Added working API OAuth callback endpoint `POST /api/auth/oauth`: Zod payload validation + create-or-reuse user by email (with safe autogenerated username/password hash) so NextAuth social sign-in callback no longer fails on missing backend route.
- 2026-04-28 — Fixed OAuth redirect loop to `/login`: NextAuth now accepts `GOOGLE/GITHUB` client IDs from both server/public env names, uses server `API_URL` fallback, and no longer blocks social sign-in when backend sync endpoint is temporarily unavailable.
- 2026-04-28 — Person page navigation clarity fix: header breadcrumb hidden for `/person/*`; local breadcrumb rendered under avatar (`Home › People › {Actor}`) in `app/person/[id]`.
- 2026-04-28 — `/watchlist` ultra interaction upgrade: implemented item pinning, manual reordering (up/down), and persisted UI preferences in localStorage; updated local watchlist model and hydration API contracts accordingly.
- 2026-04-28 — Actor SEO hardening: enriched metadata intent template + internal link cluster + person URL sitemap priority bump (`0.72`); local `verify:seo:smoke` failed only due to external sitemap timeout.
- 2026-04-28 — Created `/watchlist` page with client localStorage loading, new `GET /api/watchlist-movies` TMDB hydration route, card grid rendering, and local remove-from-watchlist action; web type-check passes.
- 2026-04-28 — `/watchlist` full upgrade shipped: new multi-media local watchlist model + legacy migration, universal `POST /api/watchlist-items` hydration for movie/series/cartoon/tvshow, advanced watchlist UI (summary/filter/search/sort/bulk), and media-aware detail-page watchlist toggles; web type-check and lints pass.
- 2026-04-28 — Actor page photos UX updated: replaced all-at-once photo grid with TMDB `/person/{id}/images` horizontal rail and left/right arrow navigation (movie-page style progressive reveal).
- 2026-04-28 — Actor page feature pass complete: added interactive filmography controls, poster-based `Known for`, clickable genres, fallback hints for missing metadata, FAQ structured data, and upgraded person credits model (`posterPath`, `genreIds`); web type-check stays green.
- 2026-04-28 — Added richer actor insights from existing TMDB data: `Zodiac`, `Years active`, and `Top genres`, plus `genreIds` support in person combined credits parsing; web type-check stays green.
- 2026-04-28 — Person page now maps TMDB external IDs to social icon links and includes a dedicated `Share profile` control near external actions; web type-check stays green.
- 2026-04-28 — Actor facts block switched from tile-like cards to textual two-column editorial layout with grouped sections and thin row dividers; web type-check remains green.
- 2026-04-28 — Added high-density actor info architecture: extended `PersonPageDetail` fields from TMDB and rendered compact facts/aliases/top-works blocks; web type-check remains green.
- 2026-04-28 — Actor page compactness pass: reduced visual noise, clarified action links, and tightened `PersonFilmography` cards for more functional scanability.
- 2026-04-28 — Refreshed actor detail UX: new profile hero layout in `app/person/[id]` and polished card-style `PersonFilmography` section; `pnpm --filter web type-check` passes.
- 2026-04-28 — Fixed actor page duplicate React keys in `PersonFilmography`: repeated `kind/workId` credits now get deterministic per-duplicate key suffixes.
- 2026-04-28 — Comments moderation is now multi-type: added `movie_comments.media_type`, API filtering by `mediaType`, and enabled comments UI under `More Like This` for movies, series, cartoons, and tv shows.
- 2026-04-28 — Added `movie_comments` schema + migration, API submit/list/moderate endpoints with Resend moderation email links, and new `MovieComments` UI block under `More Like This` (movie pages only).
- 2026-04-28 — Connected existing `MovieOverviewFeedback` below `More Like This` on movie detail pages in both streamed tail and fallback render flows.
- 2026-04-28 — Converted `TvSeriesEpisodes` to click-to-expand accordion (collapsed by default), removed modal flow, and added accessible `aria-expanded` episode toggles.
- 2026-04-28 — Reordered series detail sections so `TvSeriesEpisodes` renders immediately after cast (inside streamed below-fold TV flow); guarded legacy placement to non-streamed fallback only.
- 2026-04-28 — Fixed mobile drawer search focus artifact (purple side bars) and switched drawer shell to token-based background (`var(--bg)` mix) for homepage color parity.
- 2026-04-28 — Unified `/movie/[id]` shell background with homepage base canvas (`var(--bg)`) and reduced fixed overlay contrast to remove the "different page background" feel.
- 2026-04-28 — Closed all failing verification gates: added NextAuth typings, fixed auth route unsafe parsing/callback signatures, cleaned unused vars/handlers in web pages/components, and restored green `pnpm type-check && pnpm lint && pnpm test && pnpm build`.
- 2026-04-28 — Movie primary strip: title/director use `--color-text-*` tokens; meta uses `--text-caption`; TMDB mark (OKLCH brand) + score + MegDB wordmark/`MovieUserRatingCard` `strip` in `MoviePrimaryRatingsRow`.
- 2026-04-28 — Root `app/loading.tsx` now uses scoped `loading.module.css` with polished hero shimmer + 2 shelf skeletons (count 7) instead of 3 heavy rails with inline styles.
- 2026-04-28 — Movie primary summary polish: title/director/link color moved to primary tokens; `Watch trailer` button switched to token primary fill; `MovieUserRatingCard` strip now inline/no-wrap with MegDB dash separator next to TMDB score.
- 2026-04-28 — `MovieOverviewBlock` now always shows full synopsis (removed 4-line clamp + Read more/Read less toggle) for maximum description visibility on detail pages.
- 2026-04-28 — Restored `MovieOverviewBlock` toggle logic with default `expanded=true` (full text shown initially), and switched summary icon-circle buttons to `--color-primary*` token styling.
- 2026-04-28 — `MoviePrimarySummaryActions` now hides watchlist/share/cast controls completely; only primary `Watch trailer`/provider CTA remains.
- 2026-04-28 — Removed `MovieHeroTrailerActions` render from `MovieDetailPage` desktop sidebar under the poster (`sidebarActionsContext`), without touching primary summary action block.
- 2026-04-28 — `MovieDetailPage` hero now renders single poster on mobile even when carousel is enabled; carousel remains active for desktop/tablet via `heroDesktopBg`.
- 2026-04-28 — Removed `(hasCrewRows || metaRows.length > 0)` facts render block from `MovieDetailPage` (Director/Writers/Stars + Budget/Revenue/etc.) on both mobile and desktop.
- 2026-04-28 — `MovieCollectionSection` cards now pass `enablePointerMotion={false}` to remove spring/magnetic card motion in `More Like This` on all breakpoints.
- 2026-04-28 — Replaced hardcoded text colors with token text colors in movie detail section headers/subtext (`MovieCollectionSection`, `MovieCastSection`, `MoviePhotosSection`, `MovieWatchProvidersPanel`, and `MovieDetailPage` watch heading/lead).
- 2026-04-28 — Detail ratings: dropped OMDb + external score row; primary summary shows TMDB + user rating only; removed `MoviePrimaryRatingBrandIcons` + IMDb meta row.
- 2026-04-28 — OMDb integration hardened in `tmdb.ts` (per-request key, id normalize, parse `Ratings`); UI shows RT/Metacritic from OMDb; `OMDB_API_KEY` in root + `apps/web` `.env.example`.
- 2026-04-28 — Movie primary ratings: `MoviePrimaryRatingBrandIcon` (Simple Icons paths) + em-dash `::after` between sources; token colors for labels/separators.
- 2026-04-28 — Movie primary meta: first three genres are `next/link` to hub or `genreQueryPrefix`+id; `.metaGenreLink` / tail styles.
- 2026-04-28 — Movie `metaPlain`: larger `--text-body`, semantic text tokens + subtle separator color; middots in `.metaSep` spans.
- 2026-04-28 — Movie primary meta reverted to plain text line (`metaPlain`, middot separators); removed chips/icons/age badge styling.
- 2026-04-28 — Movie primary meta: replaced dot-separated line with bordered chips + icons; box office kicker + amount; genres chip wraps; `@container` full-width genres on very narrow meta row.
- 2026-04-28 — Movie `MoviePrimarySummaryPanel`: no 100vw bleed or gradient panel; transparent strip + hairline divider; tighter vertical rhythm; `twoCol` less top padding/gap.
- 2026-04-28 — Web TMDB env: `next.config.ts` merges repo-root `.env.local`/`.env` when unset in `apps/web`; `tmdb.ts` reads key via `getTmdbApiKey()` per fetch; root `.env.example`; offline copy mentions root or `apps/web`.
- 2026-04-28 — Homepage empty body: hero discover `try/catch` + single `HomeDiscoverShelves` with offline fallback + `.env.example` TMDB note; shelves drop `reveal`/`reveal-card` classes; global `.reveal` forced visible.
- 2026-04-28 — Homepage blank body: `.reveal` / `.reveal-card` no longer use scroll-driven `view()` timeline (time-only animation) so shelves/cards are not stuck invisible under the header.
- 2026-04-28 — Homepage: fixed root `loading.tsx` top padding (was `80svh`, looked blank); `getHeroItems` now backfills from TMDB trending week when primary hero discover yields no titles.
- 2026-04-28 — Movie detail: new `MoviePrimarySummaryPanel` + `MoviePrimarySummaryActions` replace `scoreGrid` / TMDB ring UI; `MovieShareButton` gains `iconOnly`; `MovieUserRatingCard` gains `panel` variant; removed unused score CSS from `MovieDetailPage.module.css`.
- 2026-04-28 — Movie hero carousel: `.heroImgCarouselSlide` switched from `object-fit: contain` to `cover` + slight scale so hero stills fill viewport width (no side letterboxing).
- 2026-04-28 — Movie hero carousel UX: hide hero duplicate poster when carousel; removed dots/pause/progress chrome; disabled desktop `::after` scrims for carousel class.
- 2026-04-28 — Movie hero carousel: `isolation: isolate` on `.page`, hero/ambient `z-index: 5`; new `.heroImgCarouselSlide` (no translateX/right bias); lighter carousel scrims; `AnimatePresence mode="sync"`.
- 2026-04-28 — Movie hero: raised `.hero` and `.pageAmbient` to `z-index: 2` so content sits above fixed `.page::after` scrim (was hiding carousel stills); added `.heroBackdropRevealCarousel` tint opacity for clearer slides.
- 2026-04-28 — Movie detail: stripped blur/backdrop-blur from `MovieDetailPage.module.css`, hero carousel chrome, share/trailer/photos modals; hero images use brightness only.
- 2026-04-28 — Movie/TV detail hero: added `heroBackdropStills` from TMDB images in shell and carousel UI (`MovieHeroBackdropCarousel`) when 2+ slides; fallback unchanged for 0–1 backdrops.
- 2026-04-27 — Movie detail hero update: removed desktop `Like/Dislike` block and rendered the poster in that slot.
- 2026-04-27 — Removed `.heroCopy` left accent border/padding in `MovieDetailPage.module.css` to eliminate the thin vertical purple divider on movie detail hero.
- 2026-04-27 — Desktop hero alignment pass: set `.heroLayoutNoPoster` to `--container-max` and synced inline paddings (`--space-4`, wide `--space-5`) so left poster line matches header logo line.
- 2026-04-27 — Alignment correction: updated desktop `.heroLayoutNoPoster` to asymmetric inline paddings (`start = 2x`, `end = 1x`) so poster start matches header logo X-line (wrapper+inner offsets).
- 2026-04-27 — Hero backdrop polish: increased `.heroImgCover` blur/softening and added right-side `--primary-rgb` radial tint in `.heroTint` so the right poster reads as large atmospheric blur in page color.
- 2026-04-27 — Hero color unification pass: switched `.heroTint`, `.heroVignette`, `.heroVignette::after`, and desktop/hover `.heroBackdropReveal::after` gradients from black RGBA stops to `rgba(var(--primary-rgb), ...)`.
- 2026-04-27 — Removed gray seam source: rewired `.page::after`, `.heroTint`, and `.heroVignette::after` to `color-mix(..., var(--poster-solid-tone))` so hero blur inherits body movie tone.
- 2026-04-27 — Visibility fix pass: reduced desktop hero scrim opacity, brightened/desaturated less on `.heroImgCover`, and increased `--poster-solid-tone` weight in hero/page blends to remove dull gray and reveal right poster.
- 2026-04-27 — Movie detail hero now uses explicit cinematic stack layers: backdrop image treatment, palette tint, and dedicated vignette/fade overlay for atmospheric readability.
- 2026-04-27 — Cinematic tuning pass: darkened page base, reduced tint loudness, reinforced bottom fade, and added soft dark contour shell around hero content.
- 2026-04-27 — Removed hero/page color split by disabling ambient radial page tint and replacing hero tint with neutral linear dark overlay for consistent fill.
- 2026-04-27 — Fixed `/movie/[id]` server overlay (`fetch failed`) by gating visual-enrichment POST behind explicit `NEXT_PUBLIC_API_URL` and downgrading expected network miss logging.
- 2026-04-27 — Header parity pass: converted internal header surfaces/states to neutral dark translucent glass (no independent color tint), including desktop links/buttons and mobile drawer/mega menu variants.
- 2026-04-27 — Updated movie detail shell layering: page root gradient now uses `--primary-rgb` top tint; header pill switched to neutral dark translucent blur (no independent color paint) for seamless hero-under-header blending.
- 2026-04-27 — Rebalanced movie detail background to Plex-like mood: darker base gradient + blurred low-opacity `--primary-rgb` radial accents instead of broad color wash.
- 2026-04-27 — Added fixed `.page::after` dark gradient overlay on movie detail page (top/bottom shading) above tint layer to prevent header/poster highlights from over-glowing.
- 2026-04-27 — Movie detail page now uses base dark gradient plus a fixed semi-transparent red tint overlay (`.page::before`) so film color reads as shade only.
- 2026-04-27 — Movie detail `.page` background switched from bright/full-screen fill to dark layered gradient + subtle red ambient radial overlay per UI request.
- 2026-04-27 — Step 6 detail-page shell pass: root `.page` background switched to black base + `--primary-rgb` fade (`0vh/30vh/60vh`) for seamless hero-to-body transition.
- 2026-04-27 — Step 8 blurhash utility pass: added `blurHashToDataURL` client-canvas export and kept `blurHashToDataUrl` compatibility alias with SSR-safe `document` guard in `apps/web/src/lib/blurhashToDataUrl.ts`.
- 2026-04-27 — Step 7 mobile Safari fallback: added no-blur hero image fallback when `backdrop-filter` is unsupported and `mix-blend-mode` fallback opacity for `.heroTint`.
- 2026-04-27 — Added safe `hex -> rgb` helper and injected `--primary-color` / `--primary-rgb` CSS variables at `app/movie/[id]/page.tsx` root wrapper for detail page theming.
- 2026-04-27 — Step 5 hero-top pass: movie detail hero now decodes `blur_hash` to data URL placeholder, uses stronger blurred backdrop treatment (`blur/brightness/saturate + scale`), and overlays primary-color tint for top-to-body blending.
- 2026-04-27 — Added `apps/api/src/lib/generateColors.ts`, installed `sharp`/`blurhash`/`colorthief`/`canvas`, and introduced `movies.ensureVisualMetadata` service+router path to persist `media.primary_color` and `media.blur_hash` when missing.
- 2026-04-27 — Wired E2E first-view trigger: added API route `POST /api/movie-visuals/:tmdbMovieId/ensure` and invoked it from `/movie/[id]` server page with backdrop path; API/web type-checks pass.
- 2026-04-27 — Added `primary_color` and `blur_hash` columns to `media` schema (`packages/db/src/schema/index.ts`), generated migration `drizzle/0001_outstanding_havok.sql`; migrate failed on existing `media_type` enum baseline conflict.
- 2026-04-27 — Added shared `buildWatchSeoTitle` helper and applied it globally to media detail metadata (`movie`, `cartoon`, `series/tvshow/tvshows`, `trailer`) for consistent Plex-style tab SEO titles.
- 2026-04-27 — Movie route metadata title now uses watch-intent SEO format (`Watch {Title} ({Year}) Full Movie`) to align browser tab/title behavior with Plex-style wording.
- 2026-04-27 — Movie detail background color is now derived from real poster pixels on client (`PosterDominantColor`), not from `posterPath` hashing.
- 2026-04-27 — Movie detail now renders with a plain full-page poster background only (no blur, no gradients), and hero backdrop layer is hidden.
- 2026-04-27 — Movie detail ambient background now uses poster-first source with visible full-page blur + layered dark-blue overlays (Plex-style), and root page render was re-enabled.
- 2026-04-27 — Movie detail reset step: hid the entire `MovieDetailPage` output by setting `.page` to `display: none` for staged rebuild.
- 2026-04-27 — Footer stats row alignment fix: centered `statsBar` and stat content (`statItem` + `statusRow`) so the metrics strip sits visually centered across the footer.
- 2026-04-27 — Genre breadcrumb UX pass: hidden header-level breadcrumbs on `/movies/category/*` and added local breadcrumbs under Filters button with improved readable chip styling.
- 2026-04-27 — Fixed genre-page top background offset: `/movies/category/[genre]` now uses `hub.module.css` shell (`margin-top`/`padding-top` header compensation) so background is flush to top like `/movies`.
- 2026-04-27 — `/movies/category/[genre]` now uses per-genre unique SEO intro text (`seoTitle`/`seoSubtitle`) and teaser copy from `getMovieGenreHubContent` instead of one generic template sentence.
- 2026-04-27 — Mobile drawer `Categories` now expands to nested `Browse` and `Genres` submenu (desktop-like behavior) instead of immediate route navigation.
- 2026-04-27 — `/tvshows` hardening pass: `mapTmdbTvShowRowToShelfItem` now ignores movie title/date fields; strict row gate now requires both TV name and first-air-date.
- 2026-04-27 — Added `isStrictTvShowRow` filter to `/tvshows` page and `/api/tvshows-discover` to block movie-like rows (with `title`/`release_date`) from TV Shows feed.
- 2026-04-27 — `/tvshows` label correction: removed `SERIES` from fallback genre chips in both page and API route; fallback now stays within `TV SHOW` only.
- 2026-04-27 — `/tvshows` updates: added SEO heading/subtitle, enabled `mobileGridColumns={3}`, and normalized genre chips to always two labels in both page render and `/api/tvshows-discover`.
- 2026-04-27 — `/cartoons` updates: improved SEO heading/subtitle, enabled `mobileGridColumns={3}`, and wired `enrichMovieShelfRuntime` in both page render and `/api/cartoons-discover`.
- 2026-04-27 — Desktop filters modal fit fix: in `MoviesDiscoverPage.module.css` added large-screen top anchoring and `max-height` based on `100dvh - header clearance` to prevent overlap with header.
- 2026-04-27 — `/series` fix: enabled `enrichSeriesShelfRuntime` in page + API route and normalized card genres to always output 2 chips (`SERIES/TV` fallback).
- 2026-04-27 — Updated `/series` page intro to SEO-focused heading/subtitle and enabled `mobileGridColumns={3}` so mobile shows 3 cards like `/movies`.
- 2026-04-27 — Matched `/movies` card hover behavior to homepage by passing `enablePointerMotion={false}` to discover `MediaCard` instances (same lift-style interaction, no magnetic tilt/follow).
- 2026-04-27 — Added `mobileGridColumns` prop to `MoviesDiscoverPage` and enabled `mobileGridColumns={3}` for `/movies`, forcing 3-card mobile grid only on that route.
- 2026-04-27 — Replaced generic `/movies` intro with SEO-focused heading/subtitle via new optional `seoTitle`/`seoSubtitle` props on `MoviesDiscoverPage` (used on `/movies`).
- 2026-04-27 — Fixed missing text on `/movies`: added a top SEO intro section in `MoviesDiscoverPage` (centered `h1` + subtitle) rendered above filters.
- 2026-04-27 — Replaced `/movies/category/[genre]` teaser intro with page-specific SEO subtitle copy describing this exact discover page and its filter capabilities.
- 2026-04-27 — Global palette pass: updated `globals.css` overlay/shadow/media-card color tokens from black-tinted values to blue-violet tones and replaced red/amber hardcoded score-strength scales in `MovieDetailPage.tsx`, `RandomMoviePage.tsx`, and `RegisterForm.tsx`.
- 2026-04-27 — Fixed `/movies` background not touching viewport top: added header-clearance compensation (`margin-top` + `padding-top`) on `MoviesDiscoverPage` root so background reaches the top while content remains correctly offset under fixed header.
- 2026-04-27 — Fixed missing duration in `/movies` card meta block: `apps/web/src/app/movies/page.tsx` now uses `enrichMovieShelfRuntime(...)` for initial discover items so `runtimeMinutes` is present under posters on first paint.
- 2026-04-27 — Removed Grid/Compact/List mode switch from discover UI: deleted toggle markup, persisted `discoverViewMode` logic, and `viewToggle`/`gridCompact`/`gridList` CSS in `MoviesDiscoverPage`.
- 2026-04-27 — Removed badge elements in `MediaCard`, `Header`, `Footer`, `SearchPageClient`, and `RandomMoviePage` at render level; preserved rating badges/scores and avoided global CSS regressions.
- 2026-04-27 — Added safe global badge-only hide rule in `base.css` (`[class*='badge']`) with keep guards for `rating/score/vote`; avoids previous `chip/pill` container regressions.
- 2026-04-27 — Rolled back global badge/chip/pill selector in `base.css` because it hid structural UI blocks (`*Chip*` containers), restoring normal page rendering.
- 2026-04-27 — Added global `base.css` selector gate to hide badge/chip/pill elements site-wide, with explicit keep-rules for rating/score/vote classes.
- 2026-04-27 — Tuned `MediaCard` vertical genre pills to a more compact look (smaller size and tighter spacing) without reintroducing text clipping.
- 2026-04-27 — Updated `MediaCard` genre chips to larger vertical full-width pills across desktop/mobile; removed ellipsis clipping in card metadata block under posters.
- 2026-04-27 — Unified movie detail hero genres on desktop/mobile: larger vertical stacked pills in the title/meta block, with wrap-safe labels.
- 2026-04-27 — Movie detail mobile hero genres now render as larger full-width vertical pills in the poster-adjacent meta block to prevent label clipping.
- 2026-04-27 — Lowered desktop hero content block by reducing desktop-only bottom padding in HeroSection so title/meta/CTA sit closer to the bottom edge.
- 2026-04-27 — Applied single-line hero metadata behavior to all breakpoints (desktop included): no chip wrapping, horizontal overflow enabled, hidden scrollbars; genre leading dot removal remains global.
- 2026-04-27 — Mobile hero metadata row now stays single-line with horizontal scroll (no genre wrapping), and removed leading dot prefix from genre chips.
- 2026-04-27 — Increased mobile HeroSection height clamp/min/max so overlay content no longer fully covers the poster backdrop on phones.
- 2026-04-27 — Lowered mobile HeroSection content block (title/meta/CTA) by reducing mobile-only bottom padding so controls no longer sit too high over the backdrop.
- 2026-04-27 — Added `/movies/category/[genre]/about` route (with metadata + JSON-LD + loading/error boundaries) and replaced main category hub block with compact teaser card linking to the full guide.
- 2026-04-27 — Refined `/movies/category/[genre]` editorial hub visual layout: reduced oversized paddings/chips, tightened typography rhythm, and introduced symmetric responsive card grid for cleaner alignment.
- 2026-04-27 — Hardened TMDB mosaic fetch path: wrapped discover/trending/now_playing/upcoming calls in safe fallbacks so partial TMDB 500s degrade to empty slices instead of throwing, and added a failing-first regression test for this behavior.
- 2026-04-27 — Added weekly programmatic SEO QA layer: `seo-weekly-audit.mjs` (fail/warn budgets + canonical/JSON-LD/duplicate-title/coverage signals), wired package scripts, and documented operational process in `docs/SEO_QA_RUNBOOK.md`.
- 2026-04-27 — Audited snippet QA gates: made title/description variants intent-distinct (not just word order), canonicalized `/search` metadata to root path, and aligned genre-hub breadcrumb JSON-LD to Home→Movies→Genre while disabling entity breadcrumb trail without UI equivalent.
- 2026-04-27 — Extended snippet-template cohorts to `/movies`, `/series`, `/cartoons`, and `/search` root metadata with intent-vs-entity wording variants and canonical/social alignment.
- 2026-04-27 — Added snippet-template contract for `/entity/[slug]` and `/movies/category/[genre]`: deterministic title/description variants, metadata cohort tags, and aligned social metadata.
- 2026-04-27 — Rolled out global canonical entity layer: added `/entity/[slug]` page, canonical-first `buildEntityHref`, and `/search`→`/entity` redirects for stable queries.
- 2026-04-27 — Added entity-hub SEO contracts end-to-end: sitemap-required canonical entity routes, internal-links orphan evidence checks, and static params seeds for `/entity/[slug]`.
- 2026-04-27 — Implemented Stage 4 entity interlinking across genre/movie/person surfaces with canonical genre hub links and contextual franchise/entity search links.
- 2026-04-27 — Added Stage 4 link-quality safeguards: `entitySearch` query validator and sequel/variant confidence checks for person filmography franchise clusters.
- 2026-04-27 — Added `FAQPage` JSON-LD emission for `/movies/category/[genre]` hub mode, wiring schema directly from editorial FAQ entries.
- 2026-04-27 — Added Stage 3 Programmatic SEO policy + verifier (`seo-programmatic-policy.json`, `verify-seo-programmatic-policy.mjs`) and integrated it into SEO report contracts.
- 2026-04-27 — Globalized Stage 2 genre hubs: added distinct editorial profiles for all movie genre slugs and upgraded related genres/entities/franchises to weighted semantic extraction from top hub titles (with threshold guards/fallbacks).
- 2026-04-27 — Refined genre hub quality: non-action fallback now uses genre-specific editorial profiles (not one template), and related genres/entities/franchises are computed from the current hub’s top titles plus TMDB shell metadata.
- 2026-04-27 — Added Stage 2 genre hub editorial block on `/movies/category/[genre]` with unique category narrative (intro, differentiators, subgenres, FAQ), related entities/genres/franchises, and top-title links to avoid thin-page grid-only output.
- 2026-04-27 — Refined Stage 1 entity canon spec: enforced stable-id policy across mutable entities (including franchise/collection), added trailing-slash and case normalization rules, slug-change redirect behavior, and canonical-only sitemap/internal-link contracts with explicit noindex policy for filter/thin pages.
- 2026-04-27 — Added Stage 1 SEO entity canonical specification (`docs/seo-entity-canon-stage1.md`) covering entity types, slug rules, indexability, filter-only pages, and duplicate URL resolution rules.
- 2026-04-27 — Fixed category discover regressions: removed unreleased items from genre-only feed via date upper bound and restored runtime labels by re-enabling `enrichMovieShelfRuntime` in both initial category render and `/api/discover` pagination.
- 2026-04-27 — Fixed categories genre feed inconsistency: genre-only movie discover now returns 2026+ titles in descending release-date order, and year-bucket pagination respects `primary_release_date.gte`.
- 2026-04-27 — Fixed heavy homepage refresh perception: shelves now stream without waiting for `getHeroItems`, and shelf cards render immediately via `reveal-card` (no `scroll-reveal` JS gate).
- 2026-04-27 — Accelerated discover page load: `/movies`, `/series`, `/cartoons` and matching discover APIs now return mapped shelf items directly (no per-item runtime/keyword enrichment on first paint).
- 2026-04-27 — Synced detail-page actor and poster sizing to homepage source-of-truth constants: added `lib/imageSizes.ts` and reused it in cast/collection shelves.
- 2026-04-27 — Updated header Categories mega menu: fixed center alignment to header midpoint, locked Genres-left/Browse-right ordering, and gave Browse panel a distinct tinted background/border.
- 2026-04-27 — Follow-up header fix: corrected centering drift (removed transform conflict with framer-motion) and swapped menu columns to Browse-left / Genres-right per UI feedback.
- 2026-04-27 — Added `Random Movie` to mega menu Browse items and removed standalone Random icon from header action buttons.
- 2026-04-27 — Fixed mobile header layout: switched base grid to `auto 1fr auto` and pinned `.actions` to right column so the two icons align on the far right.
- 2026-04-27 — Fixed desktop header regression by moving `.logo` / `.actions` `grid-column` constraints into `@media (max-width: 767px)` only.
- 2026-04-27 — Removed `SearchBar` focus glow (`box-shadow` on `.inputWrapper:focus-within`) to eliminate thick edge outline in header search.
- 2026-04-27 — Fixed remaining search outline: added `.input:focus-visible`/`.input:focus` `box-shadow: none` override in `SearchBar.module.css` to suppress global base focus ring.
- 2026-04-27 — Added `movieGenreRoute` mapping + new `app/movies/category/[genre]/page.tsx`; updated canonicals and header links to slug URLs; legacy `?genre=` now server-redirects to `/movies/category/<slug>`.
- 2026-04-27 — Added `app/movies/loading.tsx` + `loading.module.css` with stable hero/filter/grid skeleton to prevent broken-looking first paint during route loading.
- 2026-04-27 — Fixed size mismatch between detail `Cast` and `More Like This`: `MovieCastSection` now uses shared rail column/gap tokens (`--rail-tile-columns`/`--rail-tile-gap`) instead of local breakpoints.
- 2026-04-27 — Standardized `MovieCastSection` actor tiles to match `PopularActorsShelf`: switched to `w342`, unified `sizes`, and moved card aspect/radius to shared `--person-card-*` token variables.
- 2026-04-27 — Converted `theme/tokens` source tokens to DTCG JSON with alias references and OKLCH colors; added `dtcg.ts` resolver and kept `colors.ts` / `size.ts` as compatibility adapters.
- 2026-04-27 — Installed `@web-loom/design-core`, removed `scripts/generate-design-tokens-css.ts`, added `DesignThemeProvider` using `createTheme`/`applyTheme`, and dropped `design-tokens.generated.css` import/build hook.
- 2026-04-27 — Added `scripts/generate-token-classmaps.mjs` and switched `theme/tokens/classes.ts` to generated `classes.generated.json`; `generate:design-tokens` now regenerates both CSS tokens and class-map artifact.
- 2026-04-27 — Migrated `MediaCard` adaptive behavior from JS hook to CSS `@container` queries; added `container-type: inline-size` on card root and deleted `apps/web/src/hooks/useDeviceTier.ts`.
- 2026-04-27 — Connected `users` tRPC procedures (`register`/`login`) with Zod input + `TRPCError` mapping, and added `loginUser` in users service.
- 2026-04-27 — Mounted `appRouter` at `/trpc/*` and rewired REST auth handlers to `registerUser`/`loginUser` (no direct users Drizzle in route handlers).
- 2026-04-27 — Added `movies` domain (`queries/mutations/service`), wired `movieRouter` (`feedback`, `vote`), and switched REST `/api/movie-feedback` handlers to domain service.
- 2026-04-27 — Added users domain: `queries.ts` (`getUserByEmail`, `getUserIdByUsername`), `mutations.ts` (`insertUser`), `service.ts` (`registerUser`, Resend side-effects aligned with legacy route).
- 2026-04-27 — Added `apps/api/src/server/trpc.ts` and `context.ts` (`createTRPCContext`, `Context`, `router` / `publicProcedure` / `middleware`).
- 2026-04-27 — Added `apps/web/scripts/verify-semantic-button-states.mjs` and wired `verify:semantic:buttons` into `apps/web` lint to prevent duplicate `btn-primary` + `hover/focus/active:bg-*` classes.
- 2026-04-27 — Finalization step: added Style Dictionary + Figma sync pipeline (`tokens/style-dictionary.tokens.json`, `style-dictionary.config.json`, `scripts/sync-figma-tokens.mjs`, `pnpm run tokens:*`) and generated `theme/generated/tokens.css` + `tailwind.tokens.json`.
- 2026-04-27 — Installed `class-variance-authority` + `clsx` + `tailwind-merge`; added `apps/web/src/lib/utils.ts` `cn()` and refactored web `Button` to `cva` with embedded state behavior per variant.
- 2026-04-27 — Removed Tailwind `addComponents` button plugin and moved state variants to utility classes in `apps/web/src/components/Button/Button.tsx` (`hover:*`, `active:*`, `disabled:*`).
- 2026-04-27 — Updated `apps/web/src/components/Button/Button.tsx` to apply semantic token-backed color variants (`primary/secondary/outline/danger/warning/success`) and disabled opacity/cursor classes.
- 2026-04-27 — Extended `theme/tokens/colors.ts` (`color` aliases + `semantic`) and added Tailwind plugin utilities in `apps/web/tailwind.config.ts` (`.btn`, `.bg-bg`, `.text-primary`).
- 2026-04-27 — Mapped `theme/tokens/colors.ts` into `apps/web/tailwind.config.ts` (`theme.extend.colors`) enabling semantic color classes (`bg-bg`, `text-t1`, `bg-accent-*`).
- 2026-04-27 — Added base palette token map `theme/tokens/colors.ts` with semantic keys (`bg/card/br/ac/acg/acs/acb/t1/t2/t3`).
- 2026-04-27 — Implemented semantic `figma-token-sync` bridge: new `apps/web/src/figma/tokens.ts` (`figmaTokens` + `figmaTokenClasses`) and new card class maps (`w-card-*` / `h-card-*` / frame) exported via `apps/web/src/classes/index.ts`.
- 2026-04-27 — Updated `apps/web/tailwind.config.ts` `maxWidth`/`maxHeight` maps: semantic keys (`sm|md|lg|xl`) now point to layout tokens, enabling `max-w-lg` replacement for legacy `max-w-screen-lg`.
- 2026-04-27 — Extended `scripts/verify-tailwind-tokens.mjs` typography guards: forbid `leading-{number}` and `tracking-wide|wider|widest` in TSX className to enforce semantic lineHeight/letterSpacing token keys.
- 2026-04-27 — Updated `apps/web/scripts/verify-semantic-button-states.mjs`: for `btn-primary`, aria/data state classes must use semantic `bg-primary` (e.g. `aria-selected:bg-primary`), and script still blocks duplicate hover/focus/active background utilities.
- 2026-04-27 — Added `apps/web/src/classes/index.ts` (`@/classes`) and switched web `Button` class composition to semantic `sizeClasses` + `spacingClasses` + `borderRadiusClasses`.
- 2026-04-27 — Completed semantic cards grid runtime usage: new `CardsGrid` (`grid`, `gap-md`, `grid-cols-md`) wired into `apps/web/src/app/ui-kit/page.tsx`; kept `gap-grid-*` and `grid-cols-layout-*` compatibility aliases.
- 2026-04-27 — Locked session completion rule: every user task must be executed end-to-end in one run by default (no incremental “continue?” prompts unless explicitly requested).
- 2026-04-27 — Added semantic layout class maps in `theme/tokens/classes.ts`: `gridGapClasses` (`gap-grid-*`) and `gridColumnClasses` (`grid-cols-layout-*`), plus re-export via `apps/web/src/classes/index.ts`.
- 2026-04-27 — Migrated `packages/ui` `Button` size handling from direct `styles[size]` lookup to semantic `sizeClasses` map (no visual behavior change); ui/web type-check pass.
- 2026-04-27 — Added `theme/tokens/classes.ts` with generated `w-* h-*`, `p-*`, and `rounded-*` class maps derived from token keys in `theme/tokens/size.ts`.
- 2026-04-27 — Finished first runtime integration: web `Button` switched to `sizeClasses[size]`; web type-check passes.
- 2026-04-27 — Hardened `verify:tokens`: added guard against dynamic token utility templates in className (`w-${}`, `h-${}`, `p-${}`, `rounded-${}`); verify + web type-check pass.
- 2026-04-27 — Stage 5.2: added layout/typography tokens in `theme/tokens/size.ts`, generator emits `--ds-layout-*`, `--ds-font-weight-*`, `--ds-line-height-*`, `--ds-letter-spacing-*`; Tailwind `maxWidth/maxHeight/fontWeight/lineHeight/letterSpacing` and `app/opengraph-image.tsx` switched to tokens (tsc/lints clean).
- 2026-04-27 — Added `lineHeight.sm = 1.2` token and switched Tailwind `lineHeight`/`letterSpacing` config blocks to dynamic token-object mapping (`Object.keys(...)`), matching `maxWidth/maxHeight` token flow.
- 2026-04-27 — Finalized block: regenerated token CSS, `opengraph-image.tsx` uses `lineHeight.sm`, and `pnpm --filter web type-check` passed.
- 2026-04-27 — Removed legacy `lineHeight.tight` alias from `theme/tokens/size.ts`; regenerated token CSS; verified no `lineHeight.tight` / `--ds-line-height-tight` references and web type-check is green.
- 2026-04-27 — Stage 4 alias lock: `gap-2→xs`, `gap-4→md`, `gap-6→lg`, `gap-8→xl` in `apps/web/tailwind.config.ts`; verified `apps/web/src` has no numeric `gap-*`/`grid-cols-*` className usage.
- 2026-04-27 — Fix stretched/broken layout: Tailwind `maxWidth`/`maxHeight`/`minHeight` token keys renamed to `sz-*` so default `max-w-md` / `max-h-lg` are rem-based again; `gridTemplateColumns` layout-\* uses literal `repeat(n,…)`.
- 2026-04-27 — Stage 4 layout tokens: `gridGap` / `flexGap` / `gridColumn` + Tailwind `gap-grid-*`, `gap-flex-*`, `grid-cols-layout-*`; AST ban on numeric `grid-cols-*` / `grid-rows-*` in TSX; regenerated `design-tokens.generated.css`.
- 2026-04-27 — Single source: `generate-design-tokens-css.ts` → `design-tokens.generated.css`; globals + Tailwind bind to `--ds-*`; `verify:design-tokens-sync`; devDep `tsx`; `spaceStep` + `radiusUi` in `size.ts`.
- 2026-04-27 — Phase 3 grep playbook + `grep-output.txt`; `componentBorderRadius` + Tailwind `rounded-button`; web `Button` uses semantic radius; word-boundary audit clean under apps/packages.
- 2026-04-27 — Repo-wide `verify:tokens` + `scripts/verify-tailwind-tokens.mjs` (AST) enforces token-only sizing/spacing in TSX; root build/test/predev and husky hooks updated.
- 2026-04-27 — `buttonPadding` added to `theme/tokens/size.ts` (spacing-linked sm/md/lg); confirmed apps/web TSX free of legacy `w-|h-|p-[2468]` numeric utilities (phase 3.2).
- 2026-04-27 — SVG icon sweep: `IconSlot` module + globals `--icon-inline-15` / `--icon-size-xl`; Header/Profile/Hero/Register/Login/RandomMovie/TvSeriesEpisodes/Footer/SearchBar and movie-detail icons use token classes (tsc clean).
- 2026-04-27 — `cardSize` wired: `MediaCard`/`MediaCardSkeleton`/`MediaShelf` (+ skeleton prop), meta icons `--icon-inline-14`, Tailwind `card-*` width/height/aspect + safelist; web `Button` narrowed to `ButtonSizeKey`.
- 2026-04-27 — Tailwind `width`/`height`/min/max: numeric `8`/`16` aliased to `size.sm`/`size.lg` (with existing `12`→`size.md`); no `w-8`/`h-12` in TSX (verified grep).
- 2026-04-27 — `theme/tokens/size.ts`: semantic `buttonSize` / `iconSize` (alias `size` xs–lg), `cardSize` sm/md/lg px presets, helper key types; `size` export `as const`.
- 2026-04-27 — Tailwind: safelist `w-*`/`h-*` token keys for dynamic `` `w-${size}` `` / `` `h-${size}` ``; `Button.tsx` uses template literals + `padClasses` record.
- 2026-04-27 — MediaCard poster token pass: new `--media-card-*` tokens in `globals.css`; module uses scrim tokens, `--text-primary` / `--color-rating`, `color-mix` meta tints, `--media-card-panel-radius` for info panel corners.
- 2026-04-27 — `@repo/ui` Button: add `.secondary` / `.danger` CSS, per-variant loading ring, `fab` prop + ui-kit row (fixes broken secondary/danger class names).
- 2026-04-27 — Fix invisible shelves / blank UI: `.reveal` / `.reveal-card` use time-based animation by default, scroll `view()` only inside `@supports`; `useRevealOnScroll` re-runs on `items.length` + `IntersectionObserver` fallback.
- 2026-04-27 — Global tokens + mass CSS module pass: controls/icons/gaps → `globals.css` variables; Footer/Hero/Movie detail/Discover/Register/loading fixes; `next build` green.
- 2026-04-27 — Phase 2 sizes: `Button.tsx` token-sized classes; CSS modules swap raw 32/40px controls for `--hit-area-round-md` / `--icon-size-*` / `--space-5`; Tailwind `spacing.6` → `spacing.xl`.
- 2026-04-27 — Mapped `p-4` → `spacing.md` (12px) and `w-12`/`h-12`/min/max → `size.md` (40px) in `tailwind.config.ts`; fixed Tailwind layer directives for `next build`; `Button.animations.ts` `transition.type` `as const` for tsc.
- 2026-04-27 — Wired Tailwind (`apps/web/tailwind.config.ts`, `postcss.config.mjs`, `src/styles/tailwind.css`, layout import) to `theme/tokens/size.ts`.
- 2026-04-27 — Added `theme/tokens/size.ts` (spacing, borderRadius, fontSize, size exports).
- 2026-04-27 — Replaced `PROGRESS.md`; added `99-progress-session.mdc`, wired `CLAUDE.md` + `.cursorrules` for read/update at session boundaries.
- 2026-04-27 — Added `apps/web/src/components/Button/Button.tsx` (props-only `size` / `variant`, token-linked types) and `tsconfig` path `@/theme/*` → repo `theme/`.

---

## Phases (high level)

- [x] Phase 0 — Planning
- [x] Phase 1 — Monorepo scaffold (Turborepo, apps/web, apps/api, packages)
- [ ] Phase 2 — Database (Neon + Drizzle + migrations)
- [ ] Phase 3 — Auth (Better-Auth)
- [ ] Phase 4 — TMDB sync jobs
- [ ] Phase 5 — Public pages beyond home (detail routes, lists)
- [ ] Phase 6 — Search (Typesense or enhanced TMDB flow)
- [ ] Phase 7 — Admin tools
- [ ] Phase 8 — SEO hardening (sitemap, JSON-LD per template)
- [ ] Phase 9 — Monitoring and deploy runbooks

---

## How to maintain this file

1. **Start of session:** Read this file first (repo root: `PROGRESS.md`).
2. **After a meaningful step:** Update **Last focus**, **Next** if they changed, check phase boxes when a milestone is truly done, and add one bullet under **Recent log** with `YYYY-MM-DD — short fact`.
3. Keep notes technical only when they prevent repeat mistakes.
4. **Execution contract (strict):** Every user request is completed end-to-end in one run by default (implementation, checks, and final result). Do not split work into incremental “finish this too?” follow-ups unless the user explicitly asks for phased execution or a pause.

---

_English only. Single source of truth for “where we left off”._
